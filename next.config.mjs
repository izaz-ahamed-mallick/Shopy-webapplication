/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["wtsacademy.dedicateddevelopers.us"], // Add your domain here
    },
};

export default nextConfig;
